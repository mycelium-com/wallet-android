plugins {
    alias(libs.plugins.android.library)
    alias(libs.plugins.kotlin.android)
    alias(libs.plugins.protobuf)
}

dependencies {
    implementation libs.androidx.core
    api libs.protobuf.javalite

    // Provides google/protobuf/descriptor.proto used by types.proto's
    // `extend google.protobuf.EnumValueOptions` declaration at proto-gen time.
    // Not packaged at runtime — only consumed by protoc.
    protobuf libs.protobuf.java

    testImplementation libs.junit
}

protobuf {
    protoc {
        artifact = "com.google.protobuf:protoc:${libs.versions.protoBufVersion.get()}"
    }
    generateProtoTasks {
        all().configureEach { task ->
            task.builtins {
                java {
                    option "lite"
                }
            }
        }
    }
}

android {
    namespace 'com.satoshilabs.trezor'
    compileSdk = libs.versions.android.compileSdk.get().toInteger()

    defaultConfig {
        minSdk = libs.versions.android.minSdk.get().toInteger()
        targetSdk = libs.versions.android.targetSdk.get().toInteger()
    }

    lintOptions {
        xmlReport false
        quiet false
        showAll true
    }
    compileOptions {
        sourceCompatibility JavaVersion.VERSION_21
        targetCompatibility JavaVersion.VERSION_21
    }

    kotlinOptions {
        jvmTarget = "21"
    }
}
