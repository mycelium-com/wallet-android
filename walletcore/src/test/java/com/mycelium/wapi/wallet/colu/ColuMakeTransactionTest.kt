package com.mycelium.wapi.wallet.colu

import com.mrd.bitlib.model.Address
import com.mycelium.wapi.wallet.btc.bip44.AdditionalHDAccountConfig
import com.mycelium.wapi.wallet.btc.bip44.HDAccount
import org.junit.Before
import org.junit.Test

/*
class ColuMakeTransactionTest {
    val ColoredCoinsApiURLs = arrayOf("http://testnet.api.coloredcoins.org/v3/")
    val ColuBlockExplorerApiURLs = arrayOf("http://testnet.explorer.coloredcoins.org/api/")

    private val ACCOUNT_1_KEY = ""
    private val ACCOUNT_1_KEY = ""

    @Before
    public void setup() throws KeyCipher.InvalidKeyCipher
    {
        // create sample Colu accounts
        val coluAccount1 = Colu.getAccount(walletManager.createAccounts(AdditionalHDAccountConfig()).get(0)) as HDAccount
        val coluAccount2 = walletManager.getAccount(walletManager.createAccounts(AdditionalHDAccountConfig()).get(0)) as HDAccount
    }

    /**
     * Test that the first two addresses we generate agree with a specific seed agree with Wallet32
     */
    @Test
    fun addressGenerationTest() {
        assertEquals(Address.fromString(ACCOUNT_1_KEY), account.getReceivingAddress().get())
        assertEquals(Address.fromString(ACCOUNT_2_KEY), account.getReceivingAddress().get())
    }

}
*/