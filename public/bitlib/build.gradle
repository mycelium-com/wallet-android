apply plugin: 'java'

dependencies {
    testCompile 'junit:junit:' + junitVersion
    compile guavaArtifact
}
sourceSets {
    main {
        java {
            srcDir 'src'
        }

    }

    test {
        java {
            srcDir 'test'
        }
    }
}